// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"time"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/go-gosh/tomato/app/ent/checkpoint"
	"github.com/go-gosh/tomato/app/ent/task"
)

// CheckpointCreate is the builder for creating a Checkpoint entity.
type CheckpointCreate struct {
	config
	mutation *CheckpointMutation
	hooks    []Hook
}

// SetPoint sets the "point" field.
func (cc *CheckpointCreate) SetPoint(u uint8) *CheckpointCreate {
	cc.mutation.SetPoint(u)
	return cc
}

// SetContent sets the "content" field.
func (cc *CheckpointCreate) SetContent(s string) *CheckpointCreate {
	cc.mutation.SetContent(s)
	return cc
}

// SetDetail sets the "detail" field.
func (cc *CheckpointCreate) SetDetail(s string) *CheckpointCreate {
	cc.mutation.SetDetail(s)
	return cc
}

// SetCheckTime sets the "check_time" field.
func (cc *CheckpointCreate) SetCheckTime(t time.Time) *CheckpointCreate {
	cc.mutation.SetCheckTime(t)
	return cc
}

// SetNillableCheckTime sets the "check_time" field if the given value is not nil.
func (cc *CheckpointCreate) SetNillableCheckTime(t *time.Time) *CheckpointCreate {
	if t != nil {
		cc.SetCheckTime(*t)
	}
	return cc
}

// SetTaskID sets the "task" edge to the Task entity by ID.
func (cc *CheckpointCreate) SetTaskID(id int) *CheckpointCreate {
	cc.mutation.SetTaskID(id)
	return cc
}

// SetNillableTaskID sets the "task" edge to the Task entity by ID if the given value is not nil.
func (cc *CheckpointCreate) SetNillableTaskID(id *int) *CheckpointCreate {
	if id != nil {
		cc = cc.SetTaskID(*id)
	}
	return cc
}

// SetTask sets the "task" edge to the Task entity.
func (cc *CheckpointCreate) SetTask(t *Task) *CheckpointCreate {
	return cc.SetTaskID(t.ID)
}

// Mutation returns the CheckpointMutation object of the builder.
func (cc *CheckpointCreate) Mutation() *CheckpointMutation {
	return cc.mutation
}

// Save creates the Checkpoint in the database.
func (cc *CheckpointCreate) Save(ctx context.Context) (*Checkpoint, error) {
	var (
		err  error
		node *Checkpoint
	)
	cc.defaults()
	if len(cc.hooks) == 0 {
		if err = cc.check(); err != nil {
			return nil, err
		}
		node, err = cc.sqlSave(ctx)
	} else {
		var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
			mutation, ok := m.(*CheckpointMutation)
			if !ok {
				return nil, fmt.Errorf("unexpected mutation type %T", m)
			}
			if err = cc.check(); err != nil {
				return nil, err
			}
			cc.mutation = mutation
			if node, err = cc.sqlSave(ctx); err != nil {
				return nil, err
			}
			mutation.id = &node.ID
			mutation.done = true
			return node, err
		})
		for i := len(cc.hooks) - 1; i >= 0; i-- {
			if cc.hooks[i] == nil {
				return nil, fmt.Errorf("ent: uninitialized hook (forgotten import ent/runtime?)")
			}
			mut = cc.hooks[i](mut)
		}
		v, err := mut.Mutate(ctx, cc.mutation)
		if err != nil {
			return nil, err
		}
		nv, ok := v.(*Checkpoint)
		if !ok {
			return nil, fmt.Errorf("unexpected node type %T returned from CheckpointMutation", v)
		}
		node = nv
	}
	return node, err
}

// SaveX calls Save and panics if Save returns an error.
func (cc *CheckpointCreate) SaveX(ctx context.Context) *Checkpoint {
	v, err := cc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (cc *CheckpointCreate) Exec(ctx context.Context) error {
	_, err := cc.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (cc *CheckpointCreate) ExecX(ctx context.Context) {
	if err := cc.Exec(ctx); err != nil {
		panic(err)
	}
}

// defaults sets the default values of the builder before save.
func (cc *CheckpointCreate) defaults() {
	if _, ok := cc.mutation.CheckTime(); !ok {
		v := checkpoint.DefaultCheckTime()
		cc.mutation.SetCheckTime(v)
	}
}

// check runs all checks and user-defined validators on the builder.
func (cc *CheckpointCreate) check() error {
	if _, ok := cc.mutation.Point(); !ok {
		return &ValidationError{Name: "point", err: errors.New(`ent: missing required field "Checkpoint.point"`)}
	}
	if _, ok := cc.mutation.Content(); !ok {
		return &ValidationError{Name: "content", err: errors.New(`ent: missing required field "Checkpoint.content"`)}
	}
	if v, ok := cc.mutation.Content(); ok {
		if err := checkpoint.ContentValidator(v); err != nil {
			return &ValidationError{Name: "content", err: fmt.Errorf(`ent: validator failed for field "Checkpoint.content": %w`, err)}
		}
	}
	if _, ok := cc.mutation.Detail(); !ok {
		return &ValidationError{Name: "detail", err: errors.New(`ent: missing required field "Checkpoint.detail"`)}
	}
	if v, ok := cc.mutation.Detail(); ok {
		if err := checkpoint.DetailValidator(v); err != nil {
			return &ValidationError{Name: "detail", err: fmt.Errorf(`ent: validator failed for field "Checkpoint.detail": %w`, err)}
		}
	}
	if _, ok := cc.mutation.CheckTime(); !ok {
		return &ValidationError{Name: "check_time", err: errors.New(`ent: missing required field "Checkpoint.check_time"`)}
	}
	return nil
}

func (cc *CheckpointCreate) sqlSave(ctx context.Context) (*Checkpoint, error) {
	_node, _spec := cc.createSpec()
	if err := sqlgraph.CreateNode(ctx, cc.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{msg: err.Error(), wrap: err}
		}
		return nil, err
	}
	id := _spec.ID.Value.(int64)
	_node.ID = int(id)
	return _node, nil
}

func (cc *CheckpointCreate) createSpec() (*Checkpoint, *sqlgraph.CreateSpec) {
	var (
		_node = &Checkpoint{config: cc.config}
		_spec = &sqlgraph.CreateSpec{
			Table: checkpoint.Table,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: checkpoint.FieldID,
			},
		}
	)
	if value, ok := cc.mutation.Point(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeUint8,
			Value:  value,
			Column: checkpoint.FieldPoint,
		})
		_node.Point = value
	}
	if value, ok := cc.mutation.Content(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeString,
			Value:  value,
			Column: checkpoint.FieldContent,
		})
		_node.Content = value
	}
	if value, ok := cc.mutation.Detail(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeString,
			Value:  value,
			Column: checkpoint.FieldDetail,
		})
		_node.Detail = value
	}
	if value, ok := cc.mutation.CheckTime(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeTime,
			Value:  value,
			Column: checkpoint.FieldCheckTime,
		})
		_node.CheckTime = value
	}
	if nodes := cc.mutation.TaskIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.M2O,
			Inverse: true,
			Table:   checkpoint.TaskTable,
			Columns: []string{checkpoint.TaskColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: &sqlgraph.FieldSpec{
					Type:   field.TypeInt,
					Column: task.FieldID,
				},
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_node.task_checkpoints = &nodes[0]
		_spec.Edges = append(_spec.Edges, edge)
	}
	return _node, _spec
}

// CheckpointCreateBulk is the builder for creating many Checkpoint entities in bulk.
type CheckpointCreateBulk struct {
	config
	builders []*CheckpointCreate
}

// Save creates the Checkpoint entities in the database.
func (ccb *CheckpointCreateBulk) Save(ctx context.Context) ([]*Checkpoint, error) {
	specs := make([]*sqlgraph.CreateSpec, len(ccb.builders))
	nodes := make([]*Checkpoint, len(ccb.builders))
	mutators := make([]Mutator, len(ccb.builders))
	for i := range ccb.builders {
		func(i int, root context.Context) {
			builder := ccb.builders[i]
			builder.defaults()
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*CheckpointMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				nodes[i], specs[i] = builder.createSpec()
				var err error
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, ccb.builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, ccb.driver, spec); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{msg: err.Error(), wrap: err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.id = &nodes[i].ID
				if specs[i].ID.Value != nil {
					id := specs[i].ID.Value.(int64)
					nodes[i].ID = int(id)
				}
				mutation.done = true
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, ccb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (ccb *CheckpointCreateBulk) SaveX(ctx context.Context) []*Checkpoint {
	v, err := ccb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (ccb *CheckpointCreateBulk) Exec(ctx context.Context) error {
	_, err := ccb.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (ccb *CheckpointCreateBulk) ExecX(ctx context.Context) {
	if err := ccb.Exec(ctx); err != nil {
		panic(err)
	}
}
